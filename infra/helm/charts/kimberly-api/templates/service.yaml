apiVersion: v1
kind: Service
metadata:
  name: {{ include "kimberly-api.fullname" . }}
  labels:
    {{- include "kimberly-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kimberly-api.selectorLabels" . | nindent 4 }}
