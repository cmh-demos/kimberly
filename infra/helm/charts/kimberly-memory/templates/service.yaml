apiVersion: v1
kind: Service
metadata:
  name: {{ include "kimberly-memory.fullname" . }}
  labels:
    {{- include "kimberly-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kimberly-memory.selectorLabels" . | nindent 4 }}
