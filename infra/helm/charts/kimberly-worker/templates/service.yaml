apiVersion: v1
kind: Service
metadata:
  name: {{ include "kimberly-worker.fullname" . }}
  labels:
    {{- include "kimberly-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kimberly-worker.selectorLabels" . | nindent 4 }}
