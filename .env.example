# Kimberly Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# SECRETS MANAGEMENT
# =============================================================================

# KMS Provider selection: 'local', 'aws', 'vault', or 'sops'
# - local: File-based key storage (development only)
# - aws: AWS KMS with envelope encryption (production)
# - vault: HashiCorp Vault (enterprise/self-hosted)
# - sops: Mozilla SOPS for encrypted secrets files
KMS_PROVIDER=local

# Local KMS settings (when KMS_PROVIDER=local)
# Directory for storing keys (will be created if not exists)
LOCAL_KMS_DIR=.keys

# AWS KMS settings (when KMS_PROVIDER=aws)
# AWS_KMS_KEY_ID=alias/kimberly-cmk
# AWS_DEFAULT_REGION=us-east-1

# HashiCorp Vault settings (when KMS_PROVIDER=vault)
# VAULT_ADDR=http://127.0.0.1:8200
# VAULT_TOKEN=your-vault-token
# VAULT_MOUNT_PATH=secret
# VAULT_KEY_PATH=kimberly/encryption

# SOPS settings (when KMS_PROVIDER=sops)
# SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt
# SOPS_SECRETS_FILE=secrets.enc.yaml

# =============================================================================
# DATABASE
# =============================================================================

# PostgreSQL connection (for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/kimberly

# SQLite connection (for development)
DATABASE_URL=sqlite:///kimberly.db

# =============================================================================
# LLM CONFIGURATION
# =============================================================================

# Hugging Face token (for model downloads)
# HUGGINGFACE_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx

# Local model path (if using local models)
# LOCAL_MODEL_PATH=/models/llama-3.1-8b

# =============================================================================
# SECURITY & AUDIT
# =============================================================================

# Audit log configuration
AUDIT_LOG_FILE=logs/audit.log
AUDIT_MAX_ENTRIES=10000
AUDIT_ARCHIVE_DIR=logs/audit_archive

# Enable/disable features
FREE_MODE=true  # Blocks paid API usage

# =============================================================================
# REDIS (for session/cache)
# =============================================================================

# REDIS_URL=redis://localhost:6379/0

# =============================================================================
# TELEMETRY
# =============================================================================

# Enable anonymized telemetry
TELEMETRY_ENABLED=false
